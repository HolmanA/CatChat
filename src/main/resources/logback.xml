<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
        ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="info">
        <appender-ref ref="STDOUT"/>
    </root>

    <logger name="catchat.data.authentication.GroupMeOAuthService" level="null"/>
    <logger name="catchat.data.source.groupme" level="null"/>
    <logger name="catchat.data.source.groupme.BaseGroupMeApiCommand" level="null"/>
    <logger name="catchat.data.receiver.message" level="trace"/>
    <logger name="catchat.data.model" level="null"/>
    <logger name="catchat.data.model.Model" level="null"/>

    <!--<logger name="org.eclipse.jetty" level="INFO"/>-->
</configuration>
